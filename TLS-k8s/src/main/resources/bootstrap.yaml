server:
  port: 8080
spring:
  application:
    name: usersvc
  profiles:
    active: job
  cloud:
    consul:
      enabled: false
      host: 10.58.91.136
      port: 8500
      config:
        failFast: false
        format: YAML